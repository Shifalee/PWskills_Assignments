/*Use a nested ternary operator to check that a number is positive, negative or zero. You have to 
print “positive” if the number is positive and similarly for negative and zero also.*/

const ans = 0;
const result= ans < 0 ? "Negative" : ans > 0 ? "Positive" : "Zero";
console.log(result);